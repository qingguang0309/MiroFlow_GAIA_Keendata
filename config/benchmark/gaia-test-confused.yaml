# config/benchmark/gaia-validation.yaml
defaults:
  - default
  - _self_

name: "gaia-test-confused"

data:
  data_dir: "${data_dir}/gaia-test-confused"

execution:
  max_tasks: null  # null means no limit
  max_concurrent: 5
  pass_at_k: 1

openai_api_key: "${oc.env:OPENAI_API_KEY,???}"
